-- 0001_init.sql — placeholder, real schema arrives with the first feature.
-- Convention: numbered files NNNN_name.sql, idempotent DDL (CREATE ... IF NOT
-- EXISTS), applied in filename order with `make db-migrate`.
SELECT 1;
